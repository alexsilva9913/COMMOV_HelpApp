<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/add_point"
        android:title="Add"
        android:icon="@drawable/icon_plus"
        app:showAsAction="always"/>

    <item android:id="@+id/logout"
        android:title="Logout"
        android:icon="@drawable/logout"
        app:showAsAction="always"/>

    <item android:id="@+id/range"
        android:title="Range"
        android:icon="@drawable/range"
        app:showAsAction="always"
    />
    <item android:id="@+id/showall"
        android:title="@string/mostrarallpoints"
        android:icon="@drawable/showall_icon"
        app:showAsAction="always"
        />

</menu>